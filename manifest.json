{
  "manifest_version": 2,  // fixed

  "name": "Google APIs Redirect (To CDN)",
  "version": "0.0.2",
  "description": "Redirect request to ajax.googleapis.com to another accessible cdn server (and cancel all fonts requests).",

  "permissions": [
    "<all_urls>",
    "webRequest",
    "webRequestBlocking"
  ],

  "background": {
    "scripts": ["redirect.js"],
    "persistent": true
  }
}
